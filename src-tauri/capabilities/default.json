{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：核心事件 + 原生保存对话框（导出）",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
